# Cloud API URL - ganti dengan URL server production nanti
CLOUD_API_URL=http://localhost:3000

# Sync interval dalam detik
SYNC_INTERVAL=30

# Database credentials untuk Cloud API (docker-compose)
DB_USER=postgres
DB_PASS=password
